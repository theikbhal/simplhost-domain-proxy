# Dependencies
/node_modules
/jspm_packages/
/package-lock.json
/yarn.lock
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*

# Builds & Transpilation
/dist
/build
/out
/coverage
/es/
/lib/
/tmp/

# Environment and Config
.env
.env.*
*.config.local.*

# Editor/OS Files
.DS_Store
Thumbs.db
/.*.swp

# Logs and Data
*.log
npm-debug.log*

# Google Cloud Specific (Often handled by .gcloudignore, but good for local git)
# Credentials and configurations are generally sensitive.
# **NEVER commit service account keys (.json files) or other secrets!**
*.json
!package.json
!tsconfig.json
!jsconfig.json

# Simplhost/Proxy Specific (If they generate local files)
# You may need to add files specific to Simplhost if it creates local config/cache files outside of standard Node.js patterns.
# Example:
# /simplhost_cache/